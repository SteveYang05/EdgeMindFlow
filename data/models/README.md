# 预训练权重

`late_learn.pkl`、`late_rl.pkl` 及 `rl_candidates/` 下模型未纳入 Git 仓库。

本地运行：

```bash
python scripts/train_late_learn.py
python scripts/train_late_rl.py
```

或使用已训练好的权重文件放入本目录。后续计划：Hugging Face 发布。
