fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
httpx>=0.26.0
psutil>=5.9.0
paho-mqtt>=1.6.1
# amqtt is optional MQTT broker; not needed for HTTP-only mode
# amqtt>=0.10.0
sqlalchemy>=2.0.25
aiosqlite>=0.19.0
python-multipart>=0.0.6
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.3.0

# Optional AgentNet LLM deps (LangChain / OpenAI); falls back to rule-based if missing)
# pip install langchain langchain-openai langchain-core
