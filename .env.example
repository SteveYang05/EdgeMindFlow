# Optional LLM configuration (copy to .env and fill in; do not commit .env)
# Default rule_based mode does not require an API key

ENABLE_LLM_AGENT=0

# OpenAI-compatible API
AGENT_LLM_API_KEY=your_api_key_here
AGENT_LLM_BASE_URL=https://api.example.com/v1
AGENT_LLM_MODEL=gpt-4o-mini
